/** `object-repository/indexeddb` — the browser IndexedDB store (compiling backend with a scan fallback). */
export { IndexedDBBackend, SchemaUpgradeBlockedError } from "./IndexedDBBackend.ts";
export type { IndexedDBBackendOptions } from "./IndexedDBBackend.ts";
